import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color primaryColor = Color.fromARGB(255, 255, 178, 0);

  static const Color backgroundColor = Color.fromARGB(255, 224, 248, 250);

  static const Color surfaceColor = Colors.white;

  static const Color textPrimary = Color(0xDD000000);

  static const Color textSecondary = Color(0x8A000000);

  static const Color textHint = Color(0x61000000);

  static const Color divider = Color(0xFFE0E0E0);

  static const Color error = Colors.redAccent;
}
